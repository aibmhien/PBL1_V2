# Render Fix V0.4.1.2

- Thêm `/healthz` làm health check độc lập với template.
- Không làm ứng dụng crash nếu thư mục `demo/` chưa được đẩy lên GitHub.
- Vẫn cần đẩy đầy đủ `templates/` và `static/` để giao diện hoạt động.
- Khuyến nghị dùng GitHub Desktop để push cả cây thư mục, tránh upload web chỉ đưa các file root.
